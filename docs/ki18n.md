# `ki18n` In `ktools-javascript`

`ktools-javascript/` does not currently include a checked-in `ki18n`
implementation.

## Current Status

- the active workspace components are `kcli/` and `ktrace/`
- `ki18n` has not yet been added as a JavaScript workspace component
- future implementation docs should live in this workspace instead of the
  website layer
