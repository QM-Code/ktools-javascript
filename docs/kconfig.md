# `kconfig` In `ktools-javascript`

`ktools-javascript/` does not currently include a checked-in `kconfig`
implementation.

## Current Status

- the active workspace components are `kcli/` and `ktrace/`
- `kconfig` has not yet been added as a JavaScript workspace component
- the website keeps this page so the language/tool matrix stays complete
